alias migrate="rake db:migrate"
alias remigrate="rake db:migrate && rake db:migrate:redo && rake db:schema:dump db:test:prepare"
alias remongrate="rake mongoid:migrate && rake mongoid:migrate:redo"
alias gi="gem install"
alias ln="ln -v"
alias mkdir="mkdir -p"
alias ...="../.."
alias dropdb="rake db:drop:all && rake db:create:all && rake db:migrate && rake db:test:prepare"

# Heroku Deploy
alias deploy-staging="git push staging master && heroku run rake db:migrate --remote staging"

alias zshrc="mvim ~/.zshrc"
alias ohmyzsh="mvim ~/.oh-my-zsh"
alias aliases="mvim ~/.aliases"

# Git & Github
alias gco="git checkout"
alias gdb="git delete-branch"
alias ghb="hub browse"
alias ghco="hub checkout"
alias glog="git log --pretty=format:%s"
alias gm="git checkout master && git merge"
alias gmt="git mergetool"
alias gp="git push"
alias gpf="git push -f"
alias gpl="git pull --rebase"
alias gpr="hub pull-request"
alias gra="git rebase --abort"
alias gcb="git create-branch"
alias grc="git rebase --continue"
alias gres="git reset --hard && git clean -f -d"
alias grim="git rebase -i master"
alias grm="git rebase master"

# Bundler
alias bi="bundle install"
alias bu="bundle update"

# Nearlyfreespeech
alias sshnfs="ssh kaishin_kaishinlab@ssh.phx.nearlyfreespeech.net"

# Use updated rsync
alias rsync="/usr/local/bin/rsync"

# Launch Tower
alias tower="gittower"

# Clean launch services
alias fixopenwith='/System/Library/Frameworks/CoreServices.framework/Frameworks/LaunchServices.framework/Support/lsregister -kill -r -domain local -domain system -domain user'

# Unfollow Twitter users who don't follow back
alias unfollow="t leaders | xargs t unfollow >/dev/null"

# Projects
alias oculair="cd ~/Developer/iOS/oculair"
alias verbena="cd ~/Developer/iOS/Verbena"
alias neat-repo="cd ~/Developer/libs/neat"
alias xcode="open -a 'Xcode' ."
alias kaishinlab="cd ~/Developer/web/kaishinlab"

# Bower
alias bower='noglob bower'

# Pow
alias pow='powder'
